using Microsoft.VisualStudio.TestTools.UnitTesting;
using USBCAN;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace USBCAN.Tests
{
    [TestClass()]
    public class SecurityTests
    {
        [TestMethod()]
        public void securityAlgorithm_00Test()
        {
            //byte[] seed = { 0x55, 0x97, 0x9d, 0xbe }; Security se = new Security();
            //byte[] key = se.securityAlgorithm_00(seed);
            //byte[] re = { 0x96, 0x28, 0x5d, 0xbe };
            //Assert.AreEqual(re[0], key[0]);
            //Assert.AreEqual(re[1], key[1]);
            //Assert.AreEqual(re[2], key[2]);
            //Assert.AreEqual(re[3], key[3]);
        }
    }
}